name: Python application release
permissions:
  contents: write
  actions: read
on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'
      - name: Install PyInstaller
        run: pip install pyinstaller
      - name: Define version number
        id: vars
        run: echo ::set-output name=version::${GITHUB_REF#refs/tags/v}
      - name: Create Executable
        run: pyinstaller --onefile --noconsole --name=ServerEditor.${{ steps.vars.outputs.version }} main.py
      - name: Debug - List files in dist/
        run: ls -l dist/
      - name: Upload Executable as Artifact
        uses: actions/upload-artifact@v2
        with:
          name: server-editor
          path: dist/ServerEditor.${{ steps.vars.outputs.version }}.exe

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/download-artifact@v2
        with:
          name: server-editor
      - name: Debug - List files after download
        run: ls -l
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ steps.vars.outputs.version }}
          draft: false
          prerelease: false
      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./ServerEditor.${{ steps.vars.outputs.version }}.exe
          asset_name: ServerEditor.${{ steps.vars.outputs.version }}.exe
          asset_content_type: application/octet-stream
